{{#if recipes}}
    <h3>Your Recipes:</h3>

    <!-- For each recipe, the recipe-details partial is used to render the recipe title -->
    {{#each recipes as |recipe|}}
    {{> recipeTitles}}
    {{/each}}

{{/if}}

<button onclick="window.location.href='/new'" class="btn btn-primary">Add Recipe</button>
